#include <iostream>
#include "types.h"
#include "bitboard.h"

int main(void)
{
	// Bitboard b;

	// for (unsigned square = 0; square < 64; ++square)
	// {
	// 	b.set_bit(square);
	// 	b.print_bitboard();
	// }

	// std::cout << b.get_bitboard_value() << std::endl;

	return 0;
}
